# Tmuxinator project configs are machine-local — only the template is tracked
config/tmuxinator/*.yml
!config/tmuxinator/template.yml

# Neovim plugin lockfile is machine-local
config/nvim/lazy-lock.json
